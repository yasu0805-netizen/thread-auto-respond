import { Layout } from '@/components/Layout';
import { DataManagement } from '@/components/DataManagement';

const Data = () => {
  return (
    <Layout>
      <DataManagement />
    </Layout>
  );
};

export default Data;