import { Layout } from '@/components/Layout';
import { ThreadsIntegration } from '@/components/ThreadsIntegration';

const Integration = () => {
  return (
    <Layout>
      <ThreadsIntegration />
    </Layout>
  );
};

export default Integration;