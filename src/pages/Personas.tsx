import { Layout } from '@/components/Layout';
import { PersonasManagement } from '@/components/PersonasManagement';

const Personas = () => {
  return (
    <Layout>
      <PersonasManagement />
    </Layout>
  );
};

export default Personas;